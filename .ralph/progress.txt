## next-themes
- Installed next-themes package
- Wrapped app with ThemeProvider in app/layout.tsx (attribute="class", defaultTheme="dark", enableSystem=false)
- Added suppressHydrationWarning to html element to prevent flash
- Removed hardcoded className="dark" from html element (now managed by next-themes)
- Files changed: app/layout.tsx, package.json

## theme-toggle
- Created components/theme-toggle.tsx
- Uses useTheme from next-themes, Sun/Moon icons from lucide-react
- Uses CSS classes (hidden/block dark:hidden/dark:block) for icon switching to avoid hydration mismatch
- Files changed: components/theme-toggle.tsx

## add-toggle-to-sidebar
- Added ThemeToggle to sidebar header, right-aligned next to "Clawd" title
- Used flex justify-between for positioning
- Files changed: components/sidebar.tsx

## api-file-post
- Added writeFileContent to lib/files.ts with path traversal protection, .md check, and existence check
- Added POST handler to app/api/file/route.ts accepting {path, content} JSON body
- Validates input, returns {success: true} or appropriate error
- Files changed: lib/files.ts, app/api/file/route.ts

## edit-state
- Added isEditing and editContent state to ViewerContent
- Added Edit button (Pencil icon) in top-right of content area, visible when file loaded and not editing
- Clicking Edit initializes editContent with current content and enters edit mode
- Reset isEditing when selecting a different file
- Added refetch capability to useFileContent hook (fetchKey pattern)
- Files changed: app/page.tsx, lib/use-file-content.ts

## editor-ui
- When isEditing=true, replace MarkdownViewer with full-height textarea (monospace, dark bg via bg-muted)
- editContent initialized with file content on Edit click
- Added Save and Cancel buttons above textarea
- Cancel returns to view mode
- Files changed: app/page.tsx

## save-handler
- Implemented handleSave: POST to /api/file with path and editContent
- Shows loading spinner and "Saving..." text on Save button while saving
- Disables Cancel button while saving
- On success: exits edit mode and refetches file content
- On error: shows alert with error message
- Files changed: app/page.tsx
